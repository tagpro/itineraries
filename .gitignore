__pycache__/
*.pyc

# Scratch space Claude Code creates inside the repository: isolated worktrees
# for subagents, and the temporary command files an eval harness writes.
.claude/worktrees/
.claude/commands/
